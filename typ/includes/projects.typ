
#table(columns: (auto, auto), align: horizon, stroke: none, inset: 0pt, gutter: 10pt

    , align(top)[*2026*]
    , box[
        *Foreman*.
        University of Central Florida.
        A Python tool that enables developer to specify the commands file access permissions of phase of a project's build system, to protect these phases against pipeline poisoning attacks.
        Foreman is the tool associated with 2026 ICSE NIER paper, _Build Code is Still Code: Finding the Antidote for Pipeline Poisoning_.
    ]

    , align(top)[*2024*]
    , box[
        *Macroni*.
        Trail of Bits.
        A C static analysis tool that combines Trail of Bits’ tools PASTA and VAST to lower C preprocessor macros down to MLIR.
        #link("https://github.com/trailofbits/macroni")[GitHub].
    ]

    , align(top)[*2024*]
    , box[
        *Maki*.
        University of Central Florida.
        A C++ Clang plugin that analyzes hows C preprocessor macros affect the C AST.
        Maki provides language porting tools information they can use to port macros to target languages in a way that preserves not just macro behavior, but also macro abstractions.
        Maki is the tool associated with 2024 ICSE paper, _Semantic Analysis of Macro Usage for Portability_, for which I am the lead-author.
        #link("https://github.com/trailofbits/macroni")[GitHub].
    ]

)

